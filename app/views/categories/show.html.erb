<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> - <%= "$#{item.price.to_f/100}" %></li>
    <%= button_to "Add to Cart", cart_path(current_user) unless !current_user %>
<% end %>
</ul>

